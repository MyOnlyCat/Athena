# Copy this file to internal-postgres.env and replace every placeholder locally.
# Never commit the populated file.
ATHENA_TEST_POSTGRES_URL=postgresql+asyncpg://USERNAME:URL_ENCODED_PASSWORD@HOST:PORT/DATABASE
ATHENA_MASTER_DATABASE_SCHEMA=athena_dev
