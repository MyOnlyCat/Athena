# 更新日志

## 注册申请生命周期加固 - 2026-07-31

- Master 支持申请拒绝、可选原因和管理员恢复；拒绝后的 Node 必须等待恢复并手动重申。
- 待审批申请七天自动过期，已拒绝/已过期申请在状态变更 30 天后由后台任务清理。
- 注册入口增加 Node ID、来源 IP 和待审批容量限制，并使用单实例写锁保证原子判断。
- Node Token 增加不可逆唯一指纹；原 Token 仍加密保存且任何 API/UI 都不回显秘密。
- Node 与 Master 共享 `pending`、`approved`、`rejected`、`expired`、`restored`
  状态契约；Node 待审批每 60 秒查询，拒绝后停止自动申请并显示中文操作提示。

## Node 接入配置与审批状态修复 - 2026-07-31

### 修复

- Node 连接测试改用 Master 公共健康接口，避免未接入节点请求尚不存在的心跳路由而
  收到 404。
- 主节点配置存在未保存修改时禁用“申请接入”，申请操作只使用已经保存并应用的配置。
- 新增签名审批状态查询；Master 批准后 Node 主动同步并显示“已批准”，无需 Master
  回连。

## Node 本地数据库启动升级修复 - 2026-07-31

### 修复

- Node Windows 启动器现在会在启动 API 前执行 Alembic 数据库升级。
- 对历史 `create_all` 旧库先验证完整 `0007` 模式、生成备份并建立 Alembic 基线，
  修复新增字段后旧开发库无法启动的问题。
- 未知或残缺的无版本数据库拒绝自动建立基线，避免把不兼容结构误判为可升级旧库。

## Node 注册申请与 Master 审批 - 2026-07-31

### 新增

- Node 增加独立“申请接入”操作，使用持久化身份和本地 Token 对不含 Token 的原始
  JSON 正文签名，并持久显示“待管理员审批”。
- Master 增加身份未验证申请的原始字节暂存、管理员分页列表与 Token 二阶段审批。
- 审批成功后创建已启用接入节点，并使用 Master 凭据密钥加密保存 Node Token。

### 变更

- Node 主节点配置允许在尚未获批时保存并应用；连接测试继续作为不保存的独立操作。
- 注册 API 时间统一输出 UTC RFC 3339 `Z` 格式，管理页面按浏览器时区显示。

### 安全

- Token 不进入注册正文，不通过 API 或页面回显；错误 Token 不改变待审批申请。
- Master 审批使用接收时保存的原始字节和认证头重新验证 HMAC，避免 JSON
  重新序列化改变签名语义。

## Athena-Master 管理员账号 - 2026-07-31

### 新增

- 增加服务端分页的管理员列表，以及管理员创建、启用、禁用和密码重置 API。
- 增加中文管理员页面、状态展示、最近登录时间和敏感操作二次确认。
- 增加用户名规范化判重与 12–128 字符、字母加数字、不同于用户名的密码规则。

### 安全

- 禁止管理员禁用当前账号或最后一个可用管理员。
- 通过持久化认证版本在账号禁用或密码重置后立即撤销该账号的全部现有 JWT。

## Athena-Master 基础应用 - 2026-07-31

### 新增

- 增加与 Athena-Node 同构的 FastAPI/Alembic 与 React/Vite 项目结构。
- 增加初始化管理员、登录、当前账号、退出撤销、登录锁定和数据库健康检查。
- 增加与 Node 一致的主题令牌、明暗模式、登录布局和中文管理界面外壳。
- 增加 Windows 本地启动入口，默认使用 API `8001` 和 UI `5174`，启动前执行迁移。

### 运行边界

- 第一阶段 Master 明确限制为单进程、单 worker、单实例和本地 SQLite。
- 生产环境缺少密钥、初始化账号、数据目录或数据库配置时拒绝启动。

## 单仓库迁移 - 2026-07-30

### 变更

- 保留 Athena-Node 完整 Git 历史并将仓库根目录提升到 Athena 层级。
- 将可运行代码统一归入 `Athena-Node/`，为尚未开发的 `Athena-Master/` 增加状态说明。
- 将 API、协议、使用指南、设计规格和实施计划集中到根级 `docs/`。
- 将 Docker Compose 部署入口移到根目录并更新组件构建与挂载路径。
- 增加 Windows 快速开发启动脚本及其自检。

## 0.1.0 - 2026-07-30

### 新增

- Athena 子节点的 FastAPI、SQLite、Alembic 后端。
- 仅用户名和密码的管理员认证与用户管理。
- SSH 主机管理、加密凭据、连接测试和 TOFU 指纹确认。
- 基于 xterm.js 的二进制网页终端、应用内全屏、快捷服务器切换和 SFTP 文件管理。
- 可直接输入的远程绝对路径、三文件并发上传队列、进度、取消及浏览器下载。
- `/master-settings` 读取、连接测试和保存接口，以及对应的管理页面。
- 主节点 HMAC 签名通信、资产汇报、60 秒任务轮询和发布进度上报。
- 制品下载与 SHA-256 校验、受控并发发布执行、任务事件和审计日志。
- React + Ant Design 管理界面及 Docker Compose 一体化部署。

### 变更

- 保存的数据库主节点配置在重启后优先于环境默认值。
- 主节点设置保存前先验证候选连接，提交成功后即时替换活动客户端、同步器和执行器。
- 上传任务最多三个并发，失败不阻塞后续任务；单项、全部、切换主机和离开页面均可中止未完成请求。
- 下载从 RFC 5987 `Content-Disposition` 解析文件名，并在浏览器中保存二进制响应。

### 安全

- Argon2 密码哈希、短时 JWT、登录限速、SSH 密码 Fernet 加密。
- 一次性终端票据、SSH 主机指纹校验、制品哈希与大小限制。
- 主节点 Token 加密落库且永不出现在读取响应中；空 Token 表单安全复用已保存值。
