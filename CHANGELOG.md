# 更新日志

## 单仓库迁移 - 2026-07-30

### 变更

- 保留 Athena-Node 完整 Git 历史并将仓库根目录提升到 Athena 层级。
- 将可运行代码统一归入 `Athena-Node/`，为尚未开发的 `Athena-Master/` 增加状态说明。
- 将 API、协议、使用指南、设计规格和实施计划集中到根级 `docs/`。
- 将 Docker Compose 部署入口移到根目录并更新组件构建与挂载路径。
- 增加 Windows 快速开发启动脚本及其自检。

## 0.1.0 - 2026-07-30

### 新增

- Athena 子节点的 FastAPI、SQLite、Alembic 后端。
- 仅用户名和密码的管理员认证与用户管理。
- SSH 主机管理、加密凭据、连接测试和 TOFU 指纹确认。
- 基于 xterm.js 的二进制网页终端、应用内全屏、快捷服务器切换和 SFTP 文件管理。
- 可直接输入的远程绝对路径、三文件并发上传队列、进度、取消及浏览器下载。
- `/master-settings` 读取、连接测试和保存接口，以及对应的管理页面。
- 主节点 HMAC 签名通信、资产汇报、60 秒任务轮询和发布进度上报。
- 制品下载与 SHA-256 校验、受控并发发布执行、任务事件和审计日志。
- React + Ant Design 管理界面及 Docker Compose 一体化部署。

### 变更

- 保存的数据库主节点配置在重启后优先于环境默认值。
- 主节点设置保存前先验证候选连接，提交成功后即时替换活动客户端、同步器和执行器。
- 上传任务最多三个并发，失败不阻塞后续任务；单项、全部、切换主机和离开页面均可中止未完成请求。
- 下载从 RFC 5987 `Content-Disposition` 解析文件名，并在浏览器中保存二进制响应。

### 安全

- Argon2 密码哈希、短时 JWT、登录限速、SSH 密码 Fernet 加密。
- 一次性终端票据、SSH 主机指纹校验、制品哈希与大小限制。
- 主节点 Token 加密落库且永不出现在读取响应中；空 Token 表单安全复用已保存值。
